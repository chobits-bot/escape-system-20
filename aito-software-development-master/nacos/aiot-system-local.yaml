server:
  port: 1010

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/root?useSSL=false&useUnicode=true&characterEncoding=utf8
    username: root
    password: f31a2f47e74746a0
    hikari:
      minimum-idle: 5           # 最⼩空闲连接数
      maximum-pool-size: 20     # 最⼤连接数
      idle-timeout: 30000       # 空闲连接存活时间（毫秒）
      connection-timeout: 30000 # 连接超时时间（毫秒）
  mail:
    #smtp服务主机  qq邮箱则为smtp.qq.com; 163邮箱是smtp.163.com
    host: smtp.qq.com
    #服务协议
    protocol: smtp
    # 编码集
    default-encoding: UTF-8
    #发送邮件的账户
    username: 2172227341@qq.com
    #授权码
    password: zqxrasfdotpiecgc
    test-connection: true
    properties:
      personal: AIOT 开发团队
      subject: 欢迎来到 AIOT
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      timeout: 60s #连接空闲超过N(s秒、ms毫秒)后关闭，0为禁用，这里配置值和tcp-keepalive值一致
      lettuce:
        pool:
          max-active: 8  #允许最大连接数
          max-idle: 8    #最大空闲连接数, 默认8
          min-idle: 0    #最小空闲连接数
          max-wait: 5s   #请求获取连接等待时间

  rabbitmq:
    port: 5672
    host: 114.132.245.8
    username: thz
    password: thz
    #这个配置是保证提供者确保消息推送到交换机中，不管成不成功，都会回调
    publisher-confirm-type: correlated
    #保证交换机能把消息推送到队列中
    publisher-returns: true
    virtual-host: /
    #这个配置是保证消费者会消费消息，手动确认
    listener:
      simple:
        acknowledge-mode: manual
    template:
      mandatory: true

jwt:
  secret: zxcvbnmasdfghjuiyreqtuiwg
